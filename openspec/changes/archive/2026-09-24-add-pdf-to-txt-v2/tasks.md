# Tasks

以下勾选仅表示截至 2026-09-23 在**当前未提交工作树**中有对应实现和验证证据，不表示已发布或整份 V2 规格已验收。未勾选项必须在发布前完成；第三方 PDF 与逐字转写均保持本地，不提交二进制或受许可约束的全文。

## 1. 现有代码已实现并有验证证据

- [x] 1.1 接入 PDF → TXT 单文件转换、保留原名及拒绝 PDF → 非 TXT；`tests/test_converter.py` 的 PDF 用例证明文本 PDF 无 Pandoc 也能输出 UTF-8 TXT。
- [x] 1.2 完成 PDF 文件头/结构/加密/限额预检与按页分类；`tests/test_pdf_preflight.py`、`tests/test_pdf_classify.py` 通过。
- [x] 1.3 完成原生文字提取、质量判定、双栏/稳定三栏和保守表格恢复；`tests/test_pdf_extract.py`、`tests/test_pdf_quality.py`、`tests/test_pdf_layout.py` 以及联邦公报/清华真实样例回归通过。
- [x] 1.4 完成本地 PP-OCRv5 按页回退、竖排列序、模型缺失报错和解析/渲染 OCR 子进程超时隔离；`tests/test_pdf_ocr.py`、`tests/test_pdf_pipeline.py`、`tests/test_pdf_parse_worker.py`、`tests/test_pdf_ocr_worker.py` 通过，真实新旧扫描件均能进入 OCR。
- [x] 1.5 完成清晰网格扫描表格与 150/300 PPI × 150/200/300 DPI 对照；`tests/test_pdf_ocr_tables.py` 通过，现有记录为六组均恢复 8 行 Tab 表、39/40 单元格命中。
- [x] 1.6 建立 9 份固定来源与 SHA-256 的真实 PDF 样例清单，并运行 `scripts/evaluate_pdf_corpus.py`；本地报告显示自动门槛 9/9 通过，最初保留 4 份人工 `known-issue`，未误记为质量全过；旧书正式评分通过后改记 `pass-with-loss`，最终保留 3 份布局 `known-issue`。

## 2. 旧印刷体 90% 发布阻断项

- [x] 2.1 为 `wikimedia-chinese-scan` 的固定哈希 5 页逐页人工转写可辨认正文，事前标注排除区域并冻结本地标准答案；核验 PDF 哈希、页数与每页转写文件存在且不进入 Git。
- [x] 2.2 实现离线字符级评分与单元测试，确保评分逐页文本与最终 TXT 清理一致，并按规格做 NFKC、去排版空白、逐页 Levenshtein 距离及加权聚合；用已知插入、删除、替换和跨页错序样例验证公式与 90% 边界。
- [x] 2.3 在冻结答案上运行当前 OCR，保存聚合/逐页基线与主要错漏类别；核验结果文件包含参考哈希、模型名称、计分版本和可追溯错误样例。
- [x] 2.4 只在错误分析支持时改进旧印刷体 OCR 预处理、识别或后处理，不牺牲现代简体与 V1 路径；复测固定 5 页聚合正确率达到至少 90%，且现代扫描与 PDF 自动回归不退化。
- [x] 2.5 独立人工对照五页原图、转写和 TXT，确认排除规则未事后放宽、正文顺序可读、逐页分数无被聚合掩盖的严重失真；留下签核记录，未达 90% 不冻结 V2。

2026-09-24 阶段记录：已加入显式 `old_print` 实验模式（300 DPI + 3×3 中值去噪），并生成五页对照输出；局部字词改善与退化并存。用户要求暂不重做基准，故 2.1、2.3–2.5 保持未完成，也不宣称达到 90%。

2026-09-24 收尾记录：用户在同一固定五页上估计两模式均约 4% 错误率，但只有总体估计，尚无冻结的逐页转写、排除区域和逐页计分；五页原图与 TXT 的主要阅读顺序已核对可读，详见 `docs/validation/2026-09-24-v2-pdf-manual-closeout.md`。这不满足 2.1、2.3–2.5 的完整签核条件，相关任务保持未勾选。

2026-09-24 正式签核：项目所有者按事前协议逐页修订并确认五页参考文本，冻结后合计 5012 字；默认 `standard` 配置、`PP-OCRv5_server_rec` 按 `pdf-character-accuracy-v1` 取得 344 编辑距离、93.1365% 聚合正确率，五页分别为 94.0534%、94.0334%、93.5968%、91.1616%、92.8870%。五页均超过 90%，没有被聚合掩盖的严重失真。`old_print` 实验模式未显示明确收益，故不为追求分数切换默认配置；同次九份真实 PDF 回归 9/9 通过，现代简体扫描样例未退化。参考全文、机器输出和完整差异留在 Git 忽略目录，公开汇总见 `docs/validation/2026-09-24-old-print-formal-score.md`。

## 3. PDF 失败语义与安全验收

- [x] 3.1 补充“前页成功、后页 OCR 失败/超时/缺模型”测试；核验整份 PDF 报告失败、无输出或 ZIP 中部分 TXT、可定位时有页码，同批正常文件继续。
- [x] 3.2 补充输出写入中断、报告写入异常及重名结果测试，必要时修正原子交付/清理路径；核验原文件及旧结果哈希不变、失败不留下误导性 TXT。
- [x] 3.3 复核伪 PDF、损坏、加密、超页数/对象数/尺寸/像素及主动内容路径；运行 PDF 安全测试并检查中文错误、限额和“不执行、不联网”行为一致。

## 4. 集成、人工质量与 V1 回归

- [x] 4.1 在实际网页界面批量上传至少五个含文本/扫描/混合场景的文件并下载 ZIP；人工核验 PDF 仅可选 TXT、结果/报告完整、失败文件未进入 outputs、目录及临时文件行为正确。
- [x] 4.2 实跑 CLI 的文本型与扫描型 PDF → TXT，并在缺 Pandoc 环境下复测文本型路径；核验文件名、UTF-8、逐页报告及原文件 SHA-256 不变。
- [x] 4.3 重新运行 9 份真实 PDF 自动样例并逐页人工复核关键页；更新已知问题清单，明确旋转密集表、跨栏密集表、复杂表单/公式及图片文字的剩余限制，不用自动通过替代人工结论。2026-09-24 自动 9/9 通过；旧书正式评分后改记 `pass-with-loss`，最终保留 3 份布局 `known-issue`，详见 `docs/validation/2026-09-24-v2-pdf-manual-closeout.md`。
- [x] 4.4 重跑全部 pytest 和 V1 六路径真实示例，核验 UTF-8/GBK、Markdown 换行、图片、五文件批处理、命名避让、JSON/ZIP 与 DOCX 安全预检无回归。
- [x] 4.5 更新中文 README、V2 设计/验证记录和可发布演示样例，核验离线安装说明、90% 适用范围、已知限制及第三方 PDF/转写不入库；运行 `openspec validate add-pdf-to-txt-v2 --strict` 与 `git diff --check` 后提交用户审阅，不在本任务擅自发布。2026-09-24 自制扫描表格 PDF 已实跑并保存 TXT/报告；正式评分后终检为 122 项 pytest 通过、OpenSpec 严格校验和差异检查通过，验证结果见 `docs/validation/2026-09-24-v2-pdf-manual-closeout.md`。
