# 更新记录

## v2.0.0 — 2026-09-24

### PDF → TXT

- 新增文本型、扫描型和混合型 PDF 的本地离线 TXT 转换；文本页优先原生提取，需要时按页回退到预先准备的 PaddleOCR PP-OCRv5。
- 增加单栏、双栏、稳定三栏、保守表格与中文竖排阅读顺序处理；逐页报告记录来源、OCR 模型、置信信息、表格、警告和潜在损失。
- 增加 PDF 文件头、结构、加密、页数、对象数、页面尺寸和渲染像素预检，以及解析、渲染和 OCR 子进程超时隔离。
- 任一必要页失败时整份 PDF 失败，不交付部分 TXT；已有输出不被覆盖，同批其他文件继续转换。

### 验证与文档

- 123 项自动化测试通过，九份固定真实 PDF 自动门槛 9/9 通过，网页混合五文件批处理和 ZIP 交付通过。
- 固定五页旧印刷体在冻结人工参考上的正式字符正确率为 93.1365%，达到 90% 发布门槛；该数字不外推到其他旧书。
- 新增 OpenSpec PDF 主规格、已归档 V2 增量变更、真实 PDF 样例清单、评分协议、验证记录和可再分发的扫描表格演示。

### 已知限制

- 旋转密集表、跨栏密集表、复杂表单/公式和图片内文字仍可能错序或丢失。
- 当前仍以 Python 本地启动，不制作 macOS、Windows 或 Android 安装包。

## v1.0.1 — 2026-09-22

### 安全

- DOCX 输入在进入 Pandoc 前执行只读 ZIP 容器预检。
- 拒绝异常路径、符号链接、加密或重复条目、异常解压体积与压缩比以及损坏 CRC。
- 宏、OLE 嵌入对象和外部关系写入转换警告，不执行或主动访问相关内容。

### 文档与测试

- 增加 DOCX 安全预检设计文档、使用说明和边界测试。
- pytest 依赖版本保持不变。

## v1.0.0 — 2026-09-22

首个公开稳定版本，当前代码与转换行为在此版本冻结。

### 功能

- 支持 TXT、Markdown、DOCX 之间的六条转换路径。
- 支持批量上传、本地输出、单文件下载和 ZIP 打包下载。
- 支持 UTF-8、GBK、GB18030 等中文文本编码识别。
- 支持中文 TXT 标题、列表、简单表格和代码块规则识别。
- 提供结构统计、警告、潜在格式损失和 JSON 转换报告。
- 支持 DOCX 图片提取和本地 Markdown 图片嵌入。

### 交互

- 增加上传文件清单、源格式、大小和处理状态预览。
- 增加逐文件转换进度。
- 增加输出目录预校验、恢复默认目录和在访达中打开目录。
- 增加结果概览、详细报告、单文件下载和新批次操作。
- 源格式与目标格式相同时标记为“已跳过”，不计为转换失败。
- Streamlit 服务固定绑定 `127.0.0.1`。

### 修复

- 输出文件保留原始文件名，只替换目标扩展名。
- 压缩 Markdown 表格中 Pandoc 生成的冗长分隔线。
- Markdown 转 TXT 时保留普通单换行。
- 修复隐藏文件名与越界图片路径校验。

### 验证

- 23 项自动化测试通过。
- 六条真实转换路径通过。
- 五文件批处理、ZIP、图片往返与网页渲染测试通过。

## 后续版本

- 后续版本可继续增加格式和相应测试语料。
- 在主要格式范围基本稳定前不制作正式安装包，避免重复进行跨平台打包。
