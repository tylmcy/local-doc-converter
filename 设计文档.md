# 本地离线文档互转工具设计文档

## 1. 项目目标

开发一个面向个人使用与作品集展示的本地离线文档转换工具，在 MacBook Air M5（Apple Silicon）上运行。第一版通过 Streamlit 提供网页界面，支持 TXT、Markdown、DOCX 三种格式之间的六条转换路径，并支持批量处理、转换报告与 ZIP 打包下载。

项目不调用 AI API，不依赖在线服务，不引入数据库、账号系统或模型框架。Pandoc 负责成熟格式的读取与写出，Python 只处理编排、安全校验、中文 TXT 结构识别、报告生成和界面展示，避免自行实现 DOCX 解析器。

## 2. 范围

### 2.1 第一版包含

- TXT → Markdown、TXT → DOCX。
- Markdown → TXT、Markdown → DOCX。
- DOCX → Markdown、DOCX → TXT。
- 单文件及多文件上传，至少可处理五个文件。
- 自动检测 UTF-8、GB18030、GBK 等常见中文文本编码。
- 使用离线规则识别 TXT 中的章节标题、数字标题、短行标题、段落、列表、简单表格与缩进代码块。
- 尽可能保留标题、列表、表格、代码块、链接和图片语义。
- 输出 JSON 转换报告，界面同时显示摘要。
- 结果写入用户指定的本地输出目录，并提供单文件或 ZIP 下载。
- 每次任务使用独立临时目录，任务结束后自动清理。
- 文件名、扩展名、输出路径和上传大小校验；输出自动避让同名文件，不修改源文件。

### 2.2 第一版不包含

- DOCX 的精确版式还原。
- 页眉页脚、批注、修订记录、文本框、宏、SmartArt、目录域和复杂分页的完整保留。
- 复杂合并单元格或嵌套表格的无损转换。
- 远程图片下载；Markdown 中的网络图片只保留链接。
- OCR、PDF、电子表格、演示文稿和富文本格式。
- 常驻服务、用户系统、历史记录和云同步。

## 3. 核心设计决策

### 3.1 统一中间结构

内部统一使用 Pandoc JSON AST：

1. Markdown 与 DOCX 由 Pandoc Reader 转为 JSON AST。
2. TXT 经编码检测和中文结构规则解析后，先生成规范化 Markdown，再由 Pandoc 转为 JSON AST。这样无需在项目中绑定特定 Pandoc API 版本，也能可靠生成表格等复杂节点。
3. 清理层对 AST 做轻量规范化与统计，不破坏未知节点。
4. Pandoc Writer 将 AST 输出为 Markdown、DOCX 或纯文本。

此方案复用 Pandoc 的格式能力，让六条转换路径共享同一条主流程，也便于统一生成统计报告。

### 3.2 DOCX 处理边界

- DOCX 文档语义只通过 Pandoc 读取，不自行实现 ZIP/XML 语义解析器。
- DOCX 输入在进入 Pandoc 前只读检查 ZIP 容器安全性，包括路径、条目数量、解压体积、压缩比、加密、符号链接和 CRC；预检不解析 Word 文档语义。
- DOCX 输出由 Pandoc 生成；`python-docx` 仅用于补充文档基础样式和中文字体，不承担格式转换。
- DOCX 转 Markdown 时使用 Pandoc 的 `--extract-media` 提取内嵌图片，图片与 Markdown 一同进入结果包。

### 3.3 TXT 结构识别

规则按明确程度排序：

1. `第X章/节/篇/部分` 等中文章节模式。
2. `一、`、`（一）` 等中文序号标题。
3. `1.`、`1.1`、`1.1.1` 等数字层级标题。
4. `-`、`*`、`+`、数字序号等列表。
5. Markdown 风格管道表格和制表符分隔表格。
6. 连续缩进文本识别为代码块。
7. 前后有空行、长度较短且不以常见句末标点结尾的文本识别为低置信度标题。
8. 其他内容合并为普通段落。

低置信度推断会写入警告，避免把“尽可能识别”包装成无损结果。

### 3.4 输出目录与安全

- 浏览器上传文件先写入不可预测的任务临时目录。
- 上传文件名必须是纯文件名，清除路径成分并拒绝空名、隐藏名和不支持的扩展名。
- 设置单文件及批次大小上限，避免误上传导致内存耗尽。
- 用户输入的输出目录解析为真实路径，只允许位于当前用户主目录下，拒绝系统根目录。
- 输出始终使用新文件名；同名时自动添加递增序号，绝不覆盖已有文件或源文件。
- Pandoc 通过参数数组调用，不拼接 shell 命令；关闭网络访问逻辑，不主动获取远程资源。

## 4. 项目结构

```text
.
├── app.py                         # Streamlit 入口
├── pyproject.toml                 # 依赖、脚本和 pytest 配置
├── README.md                      # 中文使用说明
├── 设计文档.md
├── src/local_doc_converter/
│   ├── __init__.py
│   ├── cli.py                     # 命令行入口，便于测试和演示
│   ├── config.py                  # 格式、大小等配置
│   ├── errors.py                  # 可理解的业务异常
│   ├── models.py                  # 请求、结果和报告模型
│   ├── security.py                # 文件名、路径及大小校验
│   ├── docx_safety.py             # DOCX ZIP 容器安全预检
│   ├── encoding.py                # 文本编码识别
│   ├── text_parser.py             # TXT 中文结构规则与 AST 生成
│   ├── ast_tools.py               # AST 清理、统计和警告
│   ├── pandoc.py                  # Pandoc 探测和安全调用
│   ├── docx_style.py              # DOCX 基础样式补充
│   ├── converter.py               # 单文件转换编排
│   └── batch.py                   # 批处理、临时目录与打包
├── tests/
│   ├── test_encoding.py
│   ├── test_text_parser.py
│   ├── test_security.py
│   ├── test_converter.py
│   └── test_batch.py
└── examples/
    ├── input/                     # 中文示例输入
    └── output/                    # 实际转换产物与报告
```

## 5. 转换流程

```text
上传/CLI 输入
  → 文件名、扩展名、大小与目标格式校验
  → DOCX ZIP 安全预检（仅 DOCX 输入）
  → 任务临时目录
  → TXT 编码识别，或 Pandoc Reader 解析 Markdown/DOCX
  → Pandoc JSON AST
  → AST 轻量清理、结构统计和风险检查
  → Pandoc Writer 输出目标格式
  → DOCX 基础样式补充（仅 DOCX 输出）
  → JSON 报告与结果文件复制到安全输出目录
  → 单文件下载或批次 ZIP
  → 清理临时目录
```

## 6. 依赖

### 6.1 系统依赖

- macOS（目标机器为 MacBook Air M5）。
- Python 3.12 或更高版本。
- Pandoc 3.x，推荐通过 `brew install pandoc` 安装。

### 6.2 Python 依赖

- `streamlit`：本地网页界面。
- `charset-normalizer`：文本编码检测。
- `python-docx`：DOCX 输出后的基础样式补充。
- `pytest`：自动化测试。

运行时不需要联网；联网仅可能用于首次安装依赖。

## 7. 错误处理与转换报告

每个输入文件独立生成报告，字段包括：

- 源文件、目标文件、源格式、目标格式。
- 成功、跳过状态、耗时、检测到的文本编码。
- 标题、列表、表格、代码块、链接、图片数量。
- 警告、可能丢失的格式和错误原因。

批处理采用“单文件失败不阻断其他文件”的策略。界面显示可理解的中文错误；诊断细节保留在日志和报告中，不直接暴露临时路径或完整堆栈。

## 8. 风险与控制

| 风险 | 影响 | 控制措施 |
|---|---|---|
| Pandoc 未安装 | 无法执行转换 | 启动和转换前探测，给出 Homebrew 安装命令 |
| 编码误判 | 中文乱码 | BOM 优先、UTF-8 优先、charset-normalizer 候选、GB18030 回退；报告记录编码 |
| TXT 结构误判 | 标题层级不准确 | 明确规则优先，短标题规则保守，低置信度产生警告 |
| DOCX 复杂结构丢失 | 版式或对象缺失 | 报告列明限制；AST 中未知节点不主动删除 |
| DOCX 异常压缩或恶意 ZIP 路径 | 资源耗尽或越界访问 | Pandoc 前检查条目路径、数量、解压体积、压缩比、加密、符号链接和 CRC |
| 图片引用失效 | Markdown 输出缺图 | DOCX 输入提取媒体并打包；缺失本地图片产生警告；不联网抓取 |
| 路径穿越或覆盖 | 文件安全问题 | 纯文件名校验、目录边界校验、唯一文件名、参数数组调用 |
| 大文件耗尽资源 | 界面卡顿或崩溃 | 单文件与批次大小限制、顺序处理、临时目录及时清理 |

## 9. 实施步骤

1. 创建项目骨架、依赖配置和数据模型。
2. 实现安全校验、编码检测及 Pandoc 适配层。
3. 实现 TXT 中文结构识别和 Pandoc AST 统计。
4. 实现单文件转换、DOCX 样式补充、批处理和 ZIP 打包。
5. 实现 Streamlit 页面与命令行入口。
6. 编写中文 README、示例输入和自动化测试。
7. 安装依赖并运行 pytest。
8. 使用示例完成六条路径的端到端转换，检查报告和产物。

## 10. 验证策略

- 单元测试：编码检测、路径安全、DOCX 容器预检、标题与列表规则、AST 统计、报告序列化。
- 组件测试：使用可控的 Pandoc 替身验证命令编排和失败信息。
- 集成测试：本机存在 Pandoc 时执行六条真实转换路径。
- 人工验收：启动 Streamlit，批量上传至少五个文件，检查页面结果、输出目录和 ZIP。
- 回归检查：确认输入文件哈希不变、临时目录已删除、同名输出不覆盖。

## 11. 已确认的实现约束

需求范围已由用户确认，并明确要求设计完成后直接实现。因此本设计作为第一版实现基线，不再增加额外审批节点；只有出现会改变用户可见行为、依赖边界或安全模型的重大新决策时才暂停确认。

## 12. v1.0.0 冻结说明

v1.0.0 已完成六条转换路径、批处理、报告、安全控制和基础交互，并通过自动化与真实转换验证。此版本作为后续格式扩展的稳定基线，暂不制作安装包；待主要格式范围稳定后再集中处理跨平台打包，避免为中间版本重复构建与验证安装产物。

## 13. v2 PDF → TXT 开发实现

### 13.1 分层和回退规则

PDF 不使用 Pandoc AST，而是走独立的逐页流程：

```text
PDF 文件
  → 启动一次性解析子进程，父进程设置 60 秒硬超时
      → 文件头、EOF、加密、页数、对象数和页面尺寸预检
      → 根据文字数、图片数和图片覆盖率逐页分类
      → 文本页：pdfplumber 词级坐标、线框与单元格提取
      → 布局层：跨栏块、左栏、右栏与表格块排序
      → 通过任务临时 JSON 返回结果，子进程立即退出
  → 替换符、控制符、私用区字符和异常重复质量检查
  → 扫描页或低质量文字层：启动文件级 OCR 子进程
      → 一次加载 PaddleOCR PP-OCRv5 mobile
      → 按页 PDFium 200 DPI 渲染与离线识别
      → 逐页更新进度心跳，通过临时 JSON 返回结果后退出
  → 按页序合并、删除多页重复页眉页脚候选文本
  → UTF-8 TXT 与 JSON 报告
```

正常文本页不执行 OCR，避免识别降质。扫描页必须 OCR；文本页若出现明显乱码特征，也回退到 OCR。混合页的文字层正常时不对整页重复 OCR，否则原生文字与图片文字容易重复。

### 13.2 OCR 离线约束

- 固定已在 MacBook Air M5、Python 3.12 验证的 PaddlePaddle 3.3.0 和 PaddleOCR 3.7.0。
- 固定 `PP-OCRv5_mobile_det`、`PP-OCRv5_mobile_rec` 和 `PP-LCNet_x1_0_doc_ori` 三个基础模型名称与绝对路径。可显式准备 `PP-OCRv5_server_rec`；存在时优先使用，模型名称写入逐页报告。
- 日常转换在导入 Paddle 前检查包和模型目录；不完整时返回可理解错误，不触发下载。
- 模型下载只由用户显式运行 `scripts/prepare_paddleocr_models.py` 时发生。
- OCR 页面串行处理，单页渲染像素不超过 3000 万，避免在轻薄本上盲目并发造成内存峰值。
- 同一 PDF 的所有 OCR 页在同一子进程中处理，模型只初始化一次；文件完成后子进程退出并回收 Paddle 内存。

### 13.3 解析子进程超时隔离

- 每个 PDF 的预检、分类和原生提取由一个新的 Python 子进程完成，不复用上一个不可信文件的解析状态。
- 父进程不使用 shell 拼接命令；输入路径和随机临时结果路径作为参数数组传入。
- 子进程只向任务临时目录写入 JSON，父进程重建内部数据类，不反序列化不可信 pickle。
- 默认超时为 60 秒。超时时先发送终止信号，1 秒后仍未退出则强制杀死；当前文件记录可理解错误，同批次其他文件继续。
- 子进程的退出码、结果完整性和字段类型都由父进程验证。报告记录是否隔离解析以及子进程解析耗时，不向用户暴露子进程 PID。

这一层能隔离 PDF 解析卡死、原生库异常退出和超时，但不等于完整的操作系统沙箱，也不限制子进程的绝对内存上限。

### 13.4 渲染/OCR 子进程超时隔离

- 仅在存在扫描页或可疑文字层时启动 OCR 子进程；纯文本型 PDF 不导入 Paddle。
- 子进程先重新执行 PDF 预检和页码范围校验，再加载方向、检测和识别模型，避免在文件被替换时盲目渲染。
- 模型启动和预处理默认限时 30 秒。模型就绪后，每完成一页就原子更新进度 JSON，父进程为当前页重置 60 秒期限。
- 启动或单页超时时，父进程终止整个进程组；底层 PDFium/Paddle 原生库卡死、崩溃或内存不释放不会继续留在 Streamlit 主进程。
- 结果记录是否使用 OCR 隔离、子进程总耗时、启动超时和单页超时，不暴露工作进程 PID。

### 13.5 双栏与表格布局恢复

- 先从词坐标组成行块，再以页面中部的稳定横向空白作为双栏证据。左右栏都必须有足够文本，跨中缝内容不能过多。
- 跨栏标题、图注或全宽表格作为分段边界；每个分段内按“左栏从上到下，再右栏从上到下”输出。
- 表格优先使用线框和交点恢复；无框表格只在至少三个视觉列连续对齐时启用，两列无框内容不强行结构化，以避免误把双栏正文当作表格。
- TXT 表格用 Tab 分列。合并单元格保留空占位，单元格内多行文本收敛为单行；表格内文字从普通正文块剔除，避免重复。
- 报告增加双栏页码、结构化表格数、复杂表格数、表格降级数和每页布局字段。

### 13.6 当前边界与下一阶段

已完成文本型 PDF → TXT、常见双栏阅读顺序、有框/保守无框表格恢复、解析与渲染/OCR 两级子进程超时隔离、按页 OCR 回退、繁体竖排列序恢复、网页上传、批处理、报告和 ZIP 输出。尚未完成扫描表格结构识别、跨页/嵌套表格合并、三栏以上不规则布局、现代简体扫描件的分辨率/倾斜/模糊对照验收，以及子进程绝对内存上限。

### 13.7 真实 PDF 质量样例集

真实样例采用“可提交清单 + 本地二进制”的双层结构：仓库提交固定 commit 的 HTTPS 来源、SHA-256、文件大小、自动验收阈值和人工复核结论；第三方 PDF、转换结果、页面渲染图和隐私样例均由 Git 忽略。下载器先写入 `.part`，验证文件头、精确大小和哈希后再原子替换；评测器在进入解析器前再次校验完整性，并把每次结果写入独立时间戳目录。

当前九份真实 PDF 覆盖三栏文档、有框/无框/跨行表格、多页文档、主动内容警告、旋转密集表、简体中文单栏、中英混排、现代简体横排和旧式繁体竖排真实扫描件。繁体竖排扫描已利用文字框高宽比、中文占比和列中心聚类恢复主要阅读顺序；跨页重复的页边标题由边缘位置和相似度共同确认后移除，页码候选进入逐页报告。可选高精度模型在旧印刷体短语抽查中优于基础模型；错字、漏字、独有边栏文字和长网址断行仍是已知限制。

下一批仍需补充 150/300 DPI、倾斜、模糊和扫描表格对照；在完成这些覆盖并修复现有已知问题前，不宣称中文 PDF 主要场景已经通过真实样例验收。详细方案见 `docs/designs/2026-09-23-pdf-quality-corpus.md`。
