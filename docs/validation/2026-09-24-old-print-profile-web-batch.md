# 旧印刷体实验模式与网页批量验收（2026-09-24）

## 旧印刷体参数试验

- 固定源：`wikimedia-chinese-scan`，5 页，SHA-256 `42a672355b951c91324ce4f5ae7b1ddbced6c096fe5adca69c9f828c91a7a04f`。源 PDF 与全文输出仍只在 Git 忽略的本地目录保存。
- 默认 `standard`：200 DPI，原图直接送入本地 PP-OCRv5；高精度识别模型已准备时优先使用。
- 显式 `old_print`：300 DPI，识别前对 RGB 渲染图做 3×3 中值去噪；仍使用相同的离线模型、像素限额、子进程及超时。网页复选框与 CLI `--ocr-profile old_print` 均可选择，报告记录模式。
- 当前五页输出：默认 5040 字符，增强 5078 字符。这只是输出长度，**不是字符正确率**。第一页局部词语有改善，例如“教會聖節”和“製訂公禱書”；也有新漏字、杂字与仍未纠正的书名误识。提高的 OCR 置信度不能代替人工答案。
- 用户已自行做过基准，本轮按其要求不重做、也未取得可复核的冻结答案。因此 OpenSpec 的 90% 发布门槛仍未完成；不得把此试验写成已达标。

## 回归与网页批次

- `pytest -q`：122 项通过；`openspec validate add-pdf-to-txt-v2 --strict` 与 `git diff --check` 通过。
- 现代简体扫描 `yunnan-modern-scan` 在默认模式下重新转换，其 TXT SHA-256 与此前默认输出一致：`e83885174e5b94d610c47c1ea053d2b52fcf31b30ad835c379b46124b1573abc`。
- 实际 Streamlit 页面上传 6 份 PDF：文本、扫描、混合分类各有覆盖，另含 1 份故意损坏的测试文件。界面结果为成功 5、失败 1；错误说明为无效 `%PDF-` 文件头。PDF → Markdown 在界面被阻止。
- 点击 ZIP 下载后，验证压缩包无坏条目，含 `outputs/` 下 5 份 TXT、`reports/` 下 6 份 JSON 及 `batch_report.json`。失败文件没有进入 `outputs/`；输出目录无半成品，上传临时目录已清理。

上述网页与自动回归证明交付路径可用，不代表旧印刷体逐字质量已经完成正式验收。
