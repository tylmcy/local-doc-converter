# V2 PDF → TXT 阅读顺序与真实样例复核（2026-09-24）

## 范围与证据

- 环境：MacBook Air M5、Python 3.12、本地 PP-OCRv5；转换过程未调用在线服务。
- 固定样例：`quality/pdf_corpus/manifest.json` 中 9 份本地 PDF，文件完整性由评估脚本按大小和 SHA-256 检查。原文 PDF、TXT 和渲染图均在 Git 忽略目录，不随仓库分发。
- 本轮自动结果：运行 `.venv/bin/python scripts/evaluate_pdf_corpus.py`，本地运行 ID `20260924-135049-024881`，自动硬性门槛 9/9 通过。报告位于本机 `quality/pdf_corpus/results/20260924-135049-024881/quality_report.json`。自动门槛只检查成功状态、页数、最低文字量、分类、表格数及警告等，不判断语义质量。
- 人工方法：以 `pdftoppm` 渲染关键页，与本轮最终 TXT 对照页面顺序、分栏、表格行列和 OCR 可读性。不是对九份文件的逐字校对，也没有覆盖每份长文档的全部页面。

## 固定五页旧印刷体

源为 `wikimedia-chinese-scan`，SHA-256 `42a672355b951c91324ce4f5ae7b1ddbced6c096fe5adca69c9f828c91a7a04f`。五页原图均已渲染并对照本轮默认模式 TXT：

| PDF 页 | 阅读顺序复核 | 尚存问题 |
|---|---|---|
| 1 | 右侧双页的文章尾段先于左侧新文章；标题随后进入正文，整体次序吻合原图 | 标题及首段有错字，页边噪声仍可能混入 |
| 2 | 右侧从上页接续，再到左侧，未见整栏倒序 | 部分旧字误识，句内断行不稳定 |
| 3 | 右侧正文先于左侧，两页衔接可读 | 拉丁词和繁体字仍有错漏 |
| 4 | 右侧续文先于左侧分项建议，主要段落保持顺序 | 编号与标点可能误识 |
| 5 | 右到左读完整页，结尾出处在最后，未见整段反向或跨页重复 | 左侧尾段仍有错漏，竖排页边残留需人工校对 |

后续已完成事前协议要求的五页人工参考、排除范围确认、冻结哈希和正式评分。默认 `standard` 配置使用本地 server 识别模型，在 5012 个参考字符上取得 344 编辑距离、93.1365% 聚合正确率；五页分别为 94.0534%、94.0334%、93.5968%、91.1616%、92.8870%，没有被聚合掩盖的严重失真。项目所有者确认疑难字和排除范围，机器差异样例与完整结果保存在 Git 忽略目录。`old_print` 增强模式未显示明确收益，默认模式保持不变。公开证据见 [正式评分记录](2026-09-24-old-print-formal-score.md)。

## 九份真实样例的关键页人工结论

| 样例 | 本轮核对 PDF 页 | 对照结论 |
|---|---:|---|
| `federal-register-three-column` | 1、6、7 | 第 1 页主体按左中右栏输出；第 6 页上方跨栏密集表只恢复部分列关系，正文顺序大体可读；第 7 页图内信息未完整进入文字层。保留 `known-issue`。 |
| `camelot-report-table` | 1 | 正文与步骤依次输出，多行表头以 Tab 和空占位降级，主要数值行可对照。 |
| `camelot-row-span` | 1 | 县、计划、数值行序可读；合并单元格用空位表达，不能完整保留跨行关系。 |
| `camelot-hybrid-multipage` | 1、2 | 两页三列表格均有相应 Tab 行，页面顺序保持，主动内容只产生警告。 |
| `pdfplumber-rotated-table` | 1 | 原图为横置密集统计表；TXT 将表头、州名和数字聚成错列，行列关系不可读。保留 `known-issue`。 |
| `court-simplified-chinese` | 1 | 单栏公告正文、金额和日期顺序正常；印章图像不进入 TXT。 |
| `tsinghua-chinese-english-guide` | 3、6、17 | 目录和第 6 页申请说明可读，跨页大段重复未再出现；第 17 页推荐信表单字段顺序与视觉布局不完全一致，公式和局部混排仍可能错序。保留 `known-issue`。 |
| `wikimedia-chinese-scan` | 1–5 | 右页到左页、右列到左列的主要阅读顺序可读；正式正确率 93.14%，达到门槛，但仍有错漏。改记 `pass-with-loss`，不宣称无错。 |
| `yunnan-modern-scan` | 1、4 | 标题、正文和末页日期顺序可读；印章附近有杂字，长网址在行边断开，页码仍进入 TXT。 |

因此本轮完成了关键页复核，但不声称旋转密集表、跨栏表、复杂表单/公式、图片中文字已经恢复。九份自动通过与三份布局 `known-issue` 应同时保留；旧印刷体按规格改记 `pass-with-loss`。网页批量验收见 [前一记录](2026-09-24-old-print-profile-web-batch.md)。

## 文档、演示与最终检查

- README 和 V2 路线文档已注明当前稳定版仍为 `v1.0.1`、PDF 功能尚未发布、旧印刷体 90% 门槛仅适用于固定五页及其正式计分口径；`old_print` 不宣称提升准确率。
- 可发布演示输入采用项目自行生成的 `quality/pdf_corpus/generated/scanned-table-150ppi.pdf`，本轮实际转换生成 `examples/output/scanned-table-150ppi.txt` 和相应 JSON 报告：单页 OCR 成功，表格以 Tab 输出。该样例不含第三方 PDF 全文，也不代表复杂扫描表格已解决。
- 正式评分后重新运行全量 122 项 pytest、`openspec validate add-pdf-to-txt-v2 --strict` 与 `git diff --check`，均通过。`git ls-files` 未发现真实样例的 `downloads/`、`results/`、`renders/` 或本地人工参考被跟踪；隐私扫描仅命中测试中的虚构密码 `secret`，未发现真实密钥、邮箱或本机绝对路径泄露。
- 发布前终检已完成；不在本任务擅自提交、打标签、发布、归档 OpenSpec 或打包。下一步由用户审阅后决定是否归档变更并准备 V2 版本提交。
